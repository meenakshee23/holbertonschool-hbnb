## HBnB 

## Introduction to Part 2: Implementation of Business Logic and API Endpoints


This phase focuses on building:

- The Presentation Layer (API endpoints using Flask and flask-restx)
- The Business Logic Layer (models and application logic)
The goal is to turn the design from Part 1 into working code by creating the project structure and implementing features to manage users, places, reviews, and amenities.


**Install Required Packages**

In the `requirements.txt` file, list the Python packages needed for the project:

```text
flask
flask-restx
```

Install the dependencies using:

```bash pip install -r requirements.txt ```
